# Building/Compiling

Don't attempt to run `forge build` (compile), or similarly `forge test`.

It will cause an eventual memory leak and crash due to the size and amount of smart contracts to compile.

Instead simply conclude your turn with instructions to compile/build.
